// import React, { createContext, useReducer } from 'react';

// interface IThemContext {
// 	type: 'LIGHT' | 'DARK';
// }
// interface children {
// 	children: React.ReactNode;
// }

// export const ThemContext = createContext<IThemContext>({ type: 'LIGHT' });

// export default function Context({ children }: children): React.JSX.Element {
// 	const [state, dispatch] = useReducer(reducer, second);

// 	return <ThemContext.Provider>{children}</ThemContext.Provider>;
// }
